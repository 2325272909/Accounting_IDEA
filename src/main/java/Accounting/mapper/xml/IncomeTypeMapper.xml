<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Accounting.mapper.IncomeTypeMapper">

    <select id="getIncomeTypes" resultType="String" parameterType="Long">
        select income_type_name from income_type
        where user_id=#{userId}
    </select>

    <delete id="deleteItem" >
        delete from income_type
        where user_id=#{userId} and income_type_name=#{incomeTypeName}
    </delete>

</mapper>