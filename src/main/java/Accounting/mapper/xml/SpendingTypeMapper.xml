<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Accounting.mapper.SpendingTypeMapper">
    <select id="getSpendingTypes" resultType="String" parameterType="Long">
        select spending_type_name from SpendingType
        where user_id=#{userId}
    </select>

    <delete id="deleteSpendingType">
        delete from spendingType
        where user_id=#{userId} and spending_type_name = #{spendingTypeName}
    </delete>

</mapper>