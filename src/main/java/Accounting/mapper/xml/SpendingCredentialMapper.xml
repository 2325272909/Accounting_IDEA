<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Accounting.mapper.SpendingCredentialMapper">

    <select id="getSpendingCredential" resultType="String" parameterType="Long">
        select spending_credential_name from SpendingCredential
        where user_id=#{userId}
    </select>

    <delete id="deleteSpendingCredential">
        delete from spendingcredential
        where user_id=#{userId} and spending_credential_name=#{spendingCredentialName}
    </delete>

</mapper>